# this package is used for data processing 
# for processing big data pyspark is used and for data manipulation pandas is used
pyspark>=3.5.0
pandas>=2.0.0

# this package is used for testing the functionality of the code
pytest>=8.0.0

# to download the dataset
kaggle>=1.5.0

apache-airflow==2.11.2
apache-airflow[postgres]==2.11.2
psycopg2-binary
asyncpg
dbt-postgres